
Set Expansion
  - Distinct from rebalancing
  - System-level DurableTask
  - Goal here is to migrate stores to improve pool reliability and availability
  - Find pool with worst failure tolerance
    - select store to move
  - If no stores with reliability problems
    - find pool with worst availability, select store to move
  - If no reliability or availability problems, exit Task

Reassignment to a new set:
  - System-level DurableTask
  - Always reassign each store and move it
  - Keep it simple.
    - Optimize for not moving existing stores that are already in the set later
  - Durable task selects a device for a store, calls transferStore, polls for
        completion then moves to next store. On complete, update PoolState to say
        migration status is Complete

Rebalancing is a passive background task
  - System-level Durable Service
  - Only cares about keeping %usage common across all storage devices in leaf set
  - Attempts to find the best store to move from higher-usage to lower
    - discards candidate store transfer if it would hurt reliability or availability
      - if deviceFailureTolerance(): Int - stays the same or is better, move allowed
      - if hostFailureTolerance(): Int - stays the same or is better, move allowed

Need System-Level Durable Task/Service
  - Singleton service
  - By default, all hosts participate in ensuring it's running
    - Use lease-based system
      - State says how long the lease is valid for
        - Successful overwrite of object after lease expires grants ownership
          - Use NotValidBefore(existingLeaseExpiryTime) to prevent skewed host clock from taking the lease early
  - May want dedicated system management hosts for larget systems
    - Storage hosts may not have the compute or bandwidth necessary to fill the role
  - Means Executing tasks need a shutdown() method to stop execution
    - prevents collisions
    - May want custom transactUntilSuccessful that checks for this flag and exits if seen
  - To reduce load, may want to spread task execution
    - Host message to ask it to execute a task on the SystemServiceManager's behalf
      - Can poll the task state's heartbeat Key/Value pair to ensure it's alive?

Need to be careful with polling objects
  - Long offline durations could back up with thousands of reads
  - Maybe make an ObjectPoller(ptr, periodDuration, checkObjectFn)
    - Only fires off new reads if last read completed

---------

Add Inspection Commands:
  - list hosts, storage-devices, storage-pools, allocation-groups
  - Use subcommand (think this is already done)

Need some wey to list hosts & devices
  - UUIDs will be pretty meaningless
  - Add storage-devices/<link/dirname> to localDeviceName in Host state?
    - need to modify hostState state every time a device is added anyway
  - convert from Set[stoargeDeviceId] to Map[storageDeviceId, String]

Add add-host command
  - takes address & ports
  - generates a host config file in current directory

Add add-storage-device command
  - takes host-root and device dirname
  - Impl in function that can be called later by the Host
    - Maybe via a CnC message
    - Listing and configuring new devices could be valid CnC
  - Once device is initialized, create link in <hostRootDir>/storage-devices/
  - Update host to use FileSystems.getDefault().newWatchService() to monitor for additions
    - Wrap this in a helper class to simplify Host impl

Need a plan for getting the storageDevices needed for pool creation

Switch AmoebaFS to use a registered object for the root

Rename StoreManager to Host?

os.Path note:
  - can convert to java Path via os.Path.toNIO
  - then to File via java Path.toFile()

Need official mechanism for rebuilding a failed store
  - Use Rebuilding state to StoreEntry in target StorageDeviceState?
  - Simple and can be picked up by periodic host scans
    - Same as TransferIn/TransferOut
  - Rebuilder should periodically checkpoint to disk the UUID of the last restored object
    - That way can pick up from where it left off in hte event of a crash

Need a way to connect AllocationGroups to App-defined Allocators
  - Application can just create them during initialization

Consider removing usage of invalidateTransaction in favor of just throwing
exceptions.
  - May be needed in some contexts but probably ought to be avoided when
    possible
  - Makes it difficult to detect problems and recover from them
  - Hides the source of the error
  - Limited to just a single error per tx setup

Need a mechanism to handle "out of space" errors
  - Will present when we attempt to commit an object update to the store
    - Read-Only or out of space underlying media will cause it to fail
  - Will clog up the CRL if left untreated
  - Might start with just relying on TransferStore to a device with space
    - Might be able to automate this eventually
  - Could set up "spillover stores"
    - Dedicated to hosting data for full stores
    - Lots of implications though, will require some thought

How handle changing Host IP?
  - Rely on heartbeating and drop non-bootstrap HostEntries we haven't
    heard from in a long time?
  - Wait for gossip protocol implementation?

Add TLS Cert generation for hosts

Add a Quic/Netty Network implementation

----------------
- Down the road

May need to add an abortStoreTransfer method
  - to get unstuck if destination drive is full

Need a gossip protocol for spreading the word about cache invalidation for
  1. Host changed it's address/port
  2. StoreId has been migrated to a new hostState
For now we can ignore this. Hosts don't change frequently and store moves
can be found via error messages to read/write operations

Investigate hash4j
  - faster hashing alternatives
  - Consistent bucket hashing could be used for interesting distributed data structures
    - worth considering

May want to consider Apache Fory as Protobuf alternative
  - faster but not so good with unknown fields
-----------------------------------------------------------
- Brainstorming

Could add concept of EventHandlers
  - Identified by UUID
  - Optionally able to be attached to "things of interest"
    - Such as an AllocationGroup storage changing by 0.5%
  - When an "event" triggers:
    - Use TypeRegistry to get a factory for handler corresponding to UUID
      - Factory type must castable to a base factory type
        - Ensures that handler objects will have the needed API for handling events
          of that type. like AllocGroupUsageChangeEventHandler

For maximum recovery speed, DurableRecoveryManagerTask
  - Delegates restore operations to groups of workers
  - Workers restore objects
     - Store the results locally (not re-erasure coded)
       - Could use a RocksDB-like backend. Key = ObjId + PoolIndex
     - Update dist datastructure noting their location
  - End-location downloads rebulit slices as they're created

  + End-location can be throttled so as not to overload bandwidth
  + Data can be rebuilt at maximum speed (no choke points)
  + Rebuilt data can be read from dist data structure at a speed penalty

-----------------------------------------------------------
- Setup/Testing Notes

# Setting up intellij to use a JDK installed by homebrew:
# homebrew install openjdk
# Launch Intellj and open project folder
# When a JDK is not detected:
    Go to File -> Project Structure
    In SDK option, add an SDK
    Use the following path to the SDK:
        /opt/homebrew/opt/openjdk/libexec/openjdk.jdk/Contents/Home

# Run only a single test suite
sbt 'testOnly org.aspen_ddp.aspen.common.DataBufferSuite'

# Run single test. Where "min/max" identifies a test containing this substring
sbt 'testOnly *KeyValueObjectReaderSuite -- -z min/max'

# Testing commands

./t bootstrap demo/bootstrap_config.yaml

./t node demo/bootstrap_config.yaml demo/node_a.yaml
./t node demo/bootstrap_config.yaml demo/node_b.yaml
./t node demo/bootstrap_config.yaml demo/node_c.yaml

sudo launchctl start com.apple.rpcbind
./t nfs demo/cfg.yaml demo/log4j-conf.xml

On Client:
    ssh rakis@192.168.64.2
    sudo
    umount -f /mnt; mount -v -t nfs4 -o "vers=4.1" 192.168.56.1:/ /mnt

./t new-pool demo/log4j-conf.xml demo/bootstrap_config.yaml new_pool_name replication 3 2 2 node_a,node_b,node_c

./t transfer-store demo/bootstrap_config.yaml 3f1ca5f7-74be-405d-aa48-739fc23651a4:2 <target-storage-device-uuid>


-------------------------------------------------------------------------------
- Old Testing Commands
-------------------------------------------------------------------------------

# Building for demo appliction
# run "sbt pack"
# then invoke ./target/pack/bin/demo

sbt compile && rm -r demo/node_* && ./t bootstrap demo/cfg.yaml

Bootstrap Procedure:
    sbt pack
    rm -r ~/temp/aspen/*
    rm -r demo/*log
    ./target/pack/bin/demo bootstrap demo/t.yaml
    emacs demo/cfg.yaml
    ./target/pack/bin/demo amoeba demo/cfg.yaml demo/amoeba-log4j.yaml

  For each node:
    ./target/pack/bin/demo node demo/cfg.yaml node_a

  On client:
    ssh rakis@192.168.64.2
    sudo
    umount -f /mnt; mount -v -t nfs4 -o "vers=4.1" 192.168.56.1:/ /mnt

NFS Cache Clearing:
  # To free pagecache
  echo 1 > /proc/sys/vm/drop_caches

  # To free dentries and inodes
  echo 2 > /proc/sys/vm/drop_caches

  # To free pagecache, dentries and inodes
  echo 3 > /proc/sys/vm/drop_caches


Pre sbt-pack demo launch script:
#!/bin/bash

# The CLASSPATH content is generated by sbt via running the "export runtime:fullClasspath" command
#
export CLASSPATH="<sbt content>"

exec java -cp $CLASSPATH org.aspen_ddp.aspen.cmdline.Main "$@"



